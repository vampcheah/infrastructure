groups:
  - name: infrastructure
    rules:
      - alert: ContainerDown
        expr: absent(container_last_seen{name=~"infra-.*"})
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "Infrastructure container down"
          description: "Container {{ $labels.name }} has been down for more than 1 minute."
