global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'null'

receivers:
  - name: 'null'

# 如需接收告警通知，在此配置 (Telegram/Slack/Email 等)
# 示例 (Telegram):
# receivers:
#   - name: telegram
#     telegram_configs:
#       - api_url: https://api.telegram.org
#         bot_token: YOUR_BOT_TOKEN
#         chat_id: YOUR_CHAT_ID
#         message: '{{ range .Alerts }}{{ .Annotations.summary }}{{ end }}'
